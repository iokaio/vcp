# P0 portable storage spike

Original Apache-2.0 qualification code in the retained Cargo workspace.
Compare SQLite and framed files using the same records, then qualify signed,
age-encrypted full/incremental snapshots and real search-cache rebuilds.

Run [the native qualification](../../../docs/development/portable-storage-spike.md).
The published age test identity is deliberately compromised synthetic data,
never a user recovery key. This is not a VCP product command or supported format.

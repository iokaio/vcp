diff --git a/server/src/munarium-core/Cargo.toml b/server/src/munarium-core/Cargo.toml
index fab2970..cdc0010 100644
--- a/server/src/munarium-core/Cargo.toml
+++ b/server/src/munarium-core/Cargo.toml
@@ -1,21 +1,23 @@
 # SPDX-License-Identifier: Apache-2.0
+# VCP modification: join the Codex workspace; preserve explicit upstream package/dependency settings.
 [package]
+workspace = "../../../../codex/codex-rs"
 name = "munarium-core"
-version.workspace = true
-edition.workspace = true
-license.workspace = true
+version = "1.2.1"
+edition = "2021"
+license = "Apache-2.0"
 description = "The mesh kernel: append-only ledger semantics, supersession, deterministic gates, context composition. Pure: storage and retrieval live behind traits; no HTTP, no SQL, no provider calls."
 
 [dependencies]
-serde = { workspace = true }
-serde_json = { workspace = true }
-thiserror = { workspace = true }
-uuid = { workspace = true }
-sha2 = { workspace = true }
-hex = { workspace = true }
-chrono = { workspace = true }
-regex = { workspace = true }
-async-trait = { workspace = true }
+serde = { version = "1", features = ["derive"] }
+serde_json = "1"
+thiserror = "2"
+uuid = { version = "1", features = ["v4", "v7", "serde"] }
+sha2 = "0.10"
+hex = "0.4"
+chrono = { version = "0.4", default-features = false, features = ["clock", "serde"] }
+regex = "1"
+async-trait = "0.1"
 
 # Boundary rule (architecture.md §3): this crate must never depend on
 # sqlx, axum, tonic, reqwest, or utoipa. CI greps `cargo tree` for violations.
diff --git a/server/src/munarium-store-mem/Cargo.toml b/server/src/munarium-store-mem/Cargo.toml
index e8a8841..c2fc6cf 100644
--- a/server/src/munarium-store-mem/Cargo.toml
+++ b/server/src/munarium-store-mem/Cargo.toml
@@ -1,18 +1,20 @@
 # SPDX-License-Identifier: Apache-2.0
+# VCP modification: join the Codex workspace; preserve explicit upstream package/dependency settings.
 [package]
+workspace = "../../../../codex/codex-rs"
 name = "munarium-store-mem"
-version.workspace = true
-edition.workspace = true
-license.workspace = true
+version = "1.2.1"
+edition = "2021"
+license = "Apache-2.0"
 description = "In-memory StorageBackend: the conformance target, dev profile option, and the reference the Postgres backend is property-tested against."
 
 [dependencies]
 munarium-core = { path = "../munarium-core" }
-async-trait = { workspace = true }
-chrono = { workspace = true }
-serde_json = { workspace = true }
-tokio = { workspace = true, features = ["sync"] }
-uuid = { workspace = true }
+async-trait = "0.1"
+chrono = { version = "0.4", default-features = false, features = ["clock", "serde"] }
+serde_json = "1"
+tokio = { version = "1", features = ["rt-multi-thread", "signal", "macros", "sync", "time"] }
+uuid = { version = "1", features = ["v4", "v7", "serde"] }
 
 [dev-dependencies]
-tokio = { workspace = true, features = ["rt", "macros"] }
+tokio = { version = "1", features = ["rt-multi-thread", "signal", "macros", "sync", "time", "rt"] }
diff --git a/server/src/munarium-datastore/Cargo.toml b/server/src/munarium-datastore/Cargo.toml
index b2e1f4a..99f1231 100644
--- a/server/src/munarium-datastore/Cargo.toml
+++ b/server/src/munarium-datastore/Cargo.toml
@@ -1,9 +1,11 @@
 # SPDX-License-Identifier: Apache-2.0
+# VCP modification: join the Codex workspace; preserve explicit upstream package/dependency settings.
 [package]
+workspace = "../../../../codex/codex-rs"
 name = "munarium-datastore"
-version.workspace = true
-edition.workspace = true
-license.workspace = true
+version = "1.2.1"
+edition = "2021"
+license = "Apache-2.0"
 description = "Immutable, content-verified Munarium search artifacts: build, seal, verify, open, query. Independently usable — no Axum, tonic, SQLx, PostgreSQL, server config, auth or runbooks anywhere in its graph."
 
 [features]
@@ -26,11 +28,11 @@ vector-diskann = ["dep:diskann", "dep:diskann-vector"]
 artifact-file = []
 
 [dependencies]
-serde = { workspace = true }
-serde_json = { workspace = true }
-sha2 = { workspace = true }
-hex = { workspace = true }
-thiserror = { workspace = true }
+serde = { version = "1", features = ["derive"] }
+serde_json = "1"
+sha2 = "0.10"
+hex = "0.4"
+thiserror = "2"
 tantivy = { version = "0.22", optional = true }
 # =exact pins, not ranges: the DataProvider/glue trait ensemble this adapter
 # implements is public API whose shape the contract fixture test asserts.

# P8-05 portable owner fixture proposal — revision 3, Node verification increment 1, license increment 1

Status: **PROPOSAL — awaiting owner approval; no paid execution authorized by this packet.**

Three newly authored synthetic tasks, each proposed once on SQLite and once on Files, produce six planned first attempts. These fixtures have not been sent to a provider. Author/oracle inspection is not a live model attempt and does not create statistical independence between the two backend repetitions. Results would describe three scenarios, not a representative software-engineering benchmark.

Only the selected task's `workspace/` and `task.txt` are model-visible. `hidden/`, other cases, the U01 second workspace, oracle/reference code, setup receipts, fixture inventory and this proposal remain outside every model-visible root and prompt. The offline `tools/prepare.cjs` bootstrap copies only manifest-whitelisted workspace files into six fresh private roots outside the repository. It does not copy hidden truth, other cases or source repository metadata. Never run a model against this source proposal inside the VCP checkout. Do not give the model the entire proposal directory.

Proposed numeric gates, fixed before execution: one first attempt per scenario/backend; USD 8.000000 total root cap per attempt; USD 48.000000 aggregate ceiling; 900-second overall task deadline; at most 16 model requests and 16384 total output tokens per request within a freshly qualified provider's limits; at most 360 seconds per response clamped to remaining task time; zero transport retries, reruns or model graders. Root cost includes all children/support/failed calls. Unknown charges stop subsequent dispatch and retain their bound; they never count as free. Human interventions are recorded and must be zero for the automated task-success gate. A new trial allocation may be admitted only within the existing campaign's remaining headroom.

These are proposals, not approved quality/cost/latency thresholds. The initial USD 1/task draft could not admit the existing Qwen 3.8 conservative USD 6.492808 per-request bound. It was superseded before any live attempt by this proposed USD 8/task allocation; its inventory and changed documents are preserved in the sibling owner-fixtures-proposal-one-dollar-history directory. No profile or campaign ledger was changed. Do not weaken reservation admission. Luna qualification has expired and must be refreshed before selecting it. Bind exact provider/catalog/profile and the final installed artifact separately before running; no provider is selected by these fixtures.

U01: find the single seeded architecture violation with exact observed source citations, classify generated code correctly, zero unsupported findings and zero foreign-workspace claims; unchanged workspace. U02: identify both seeded correctness defects, their triggers and introduced-by-diff causality; zero false positives, duplicate/unqualified findings, writes or invented executed checks. U03: all hidden feature assertions, visible tests, exact allowed-file scope, staged/unstaged/untracked preservation and current integrated-parent verification pass. Human explanation/usefulness/architecture-fit rubrics are recorded separately and remain pending.

The paired backend schedule is U01 SQLite, U01 Files, U02 SQLite, U02 Files, U03 SQLite, U03 Files. A failed task remains in the denominator. No prompt/fixture/rubric adjustment is allowed within this frozen revision. Do not enable automatic routing defaults from these six observations.

U01 contains nested instructions, generated code, an intentional module-boundary violation and an isolated similar-name second workspace. U02 declares the correct Git base overlay, current source changes and unrelated user note; the bootstrap generates change.diff from that base in each private workspace. U03 declares a two-module feature and baseline/staged/unstaged/untracked user-edit state; the bootstrap reconstructs that state. No .git directory, generated diff, setup receipt or self-check result is part of this source package. Fresh private setup receipts bind actual Git HEAD/index/status and source hashes for each future attempt.

Each hidden human rubric has three criteria, proposed as three required satisfied criteria out of three, assessed by the owner and recorded separately from executable scores. U01's future execution setup must populate separately scoped memory for the second workspace before testing forbidden recall; merely keeping a second directory does not establish memory isolation.

The oracle self-check confirms the planted U02 defects, validates the U03 reference solution against the hidden oracle and proves the incomplete starting implementation fails. Run `node hidden/selfcheck.cjs` from this directory; retain its output outside the source fixture tree. This only validates the fixture. It does not qualify VCP. The hidden U03 oracle must later execute against the integrated current parent, not a child-only patch.

See `hidden/provider-budget-analysis.json` for the exact Qwen reservation arithmetic. The six proposed owner slots total USD 48; a separately proposed single interactive pause/resume slot of USD 16 permits two USD 6.492808 reservations while the first remains uncertain. Combined proposed slots total USD 64 against observed campaign headroom of USD 64.594570. Reconcile the ledger before launch; no repeats are funded automatically and none of these allocations has been applied to an actual profile or campaign ledger.

This proposal does not satisfy actual routing, memory, MCP/skills, visible child execution, optimization, interactive U06, U07 or the other integrated owner requirements. Those need separately mapped evidence. Machine handoff remains skipped; physical full-volume exhaustion remains an explicit recovery gap. Only the owner can record final acceptance.

To prepare without inference: `node tools/prepare.cjs <new-private-directory> <pinned-git-executable>`. The destination must not exist and must be outside this fixture tree and every enclosing Git repository. The bootstrap is one-shot; retain failed preparation roots rather than replaying or overwriting them. The returned preparation.json contains a proposed schedule and actual workspace hashes, not execution permission, provider qualification or an approved spend reservation. No model call occurs.

Revision 3 supersedes the portable packaging of the prior 46-file proposal inventory SHA-256 5f46c615139ea2a2dea33cab926a10503c884926df8b496e7feafe449b103830. That prior freeze remains unchanged under artifacts. Prompts, seeded source behavior, proposed USD 8/task limits and hidden oracle are unchanged; source metadata and reconstruction are new and require the new manifest identity. Proposed tracking destination: src/evals/release/p8-owner-v3/.

The proposed `p8-owner-v3-node-verification-1` increment adds the minimal U03 package.json test declaration required by VCP's Node check discovery and a qualification-only argument-restricted launcher source. It supersedes manifest SHA-256 86b7b39cba7ee000d3913f50245f8f7f6c0418fc3a5b1eac26a64f053b0282cb; that exact portable v3 packet remains retained under artifacts/production-draft/owner-fixtures-portable-v3. The launcher preserves the existing builtin-generation-launcher.rs permission fence; only the allowed test file and embedded Node/SystemRoot variable names change. The generation editable set remains the two source modules: package.json and tests must be preserved.

Before a generation trial, separately build and hash `tools/page-check-launcher.rs` with `VCP_P805_NODE` bound to the qualified pinned Node executable and `VCP_P805_SYSTEMROOT` to the tested Windows SystemRoot. Retain compiler/source/binary/runtime build evidence. Its sole allowed arguments are `--test --test-reporter=tap --test-concurrency=1 test/page.test.cjs`. It runs Node with the existing permission fence, workspace-only filesystem read, no process/network/write grants, and a cleared environment. The trusted VCP process/check profile must explicitly bind this sole launcher and use the existing conservative opaque-effect classification; this source packet grants no execution authority or automatic permission changes. Hidden-oracle assessment remains separate from these visible current-parent Node checks.

The proposed `p8-owner-v3-node-verification-1-spdx-1` revision adds missing Apache-2.0 SPDX comments to the existing first lines of original CommonJS sources, as CONTRIBUTING.md requires. No source line is inserted, so frozen finding/citation line numbers remain unchanged. This license-only proposal revision supersedes manifest SHA-256 c39ab2d89dff5bb08b4cf8f3e8365a56420ce169e54c579dd5b496412256ba0b; that complete packet remains preserved at artifacts/production-draft/owner-fixtures-node-verification-1. Prompts, quality/cost/latency gates, launcher bytes and runtime behavior are unchanged. Source hashes and newly materialized Git baselines change with the comments and must bind the new manifest before any owner trial.

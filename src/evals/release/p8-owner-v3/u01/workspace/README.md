# Meter report service

GET /summary enters src/transport/handler.cjs, which calls the application service. The in-memory repository returns meter readings; domain code computes their summary. Generated API examples are rebuilt from an external schema and are not active modules. No model or external network is required to inspect this fixture.

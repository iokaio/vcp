# Domain guidance

Keep calculation policy independent of transport, app and storage. Domain imports may target domain/ or shared/ only. summarizeReadings(readings) returns an integer total and a presentation label. Cite the import that establishes any claimed boundary violation; do not infer active imports from generated examples.

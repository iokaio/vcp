# Retained unexecuted revision 2

These are the exact fixture and comparison metadata before revision 3 clarified
the synthetic pagination/cancellation API. Revision 2 records zero model calls;
this directory contains no execution or qualification receipt.

Source and oracle paths in this manifest resolve relative to the developer
fixture root, not this archive directory. All referenced project/oracle files
remain unchanged there, including `MCP-boundary-pages-v1`. The current manifest
selects `MCP-boundary-pages-v2` instead. Shared comparison bytes are retained here;
the shared rubric and authoring samples remain unchanged at the fixture root.
The current oracle intentionally accepts only the current fixture revision.

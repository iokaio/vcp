# Retained unexecuted revision 3

These exact fixture and comparison metadata precede revision 4. Revision 3
records zero model calls; this directory is not an execution or qualification
receipt. Source and oracle paths resolve relative to the developer fixture root.

All original referenced project and oracle bytes remain there unchanged. Revision
4 selects new v2 case IDs for the five revised tools, REST, request and streaming
contracts. The thirteen other cases, including MCP-boundary-pages-v2, are unchanged.
The shared comparison bytes are retained here; the rubric and authoring samples
remain unchanged at the fixture root. The structural oracle loads only the current
revision.

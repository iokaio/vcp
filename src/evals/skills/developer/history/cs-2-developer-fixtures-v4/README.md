# Retained unexecuted revision 4

These exact fixture and comparison metadata precede revision 5. Revision 4
records zero model calls; this directory is not an execution or qualification
receipt. Source and oracle paths resolve relative to the developer fixture root.

All original referenced project and oracle bytes remain there unchanged, and the
shared `rubric.json` (revision 1) remains at the fixture root. Revision 5 selects
new case IDs for all eighteen cases because every prompt, tool allowlist and write
project changed. The structural oracle loads only the current revision.

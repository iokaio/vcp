# CS-1 prospective follow-up inputs

Status: frozen fixture revision 1; **unapproved and unexecuted**. Four original normal tasks, two for each CS-1 candidate. These are prospective inputs, not a live-campaign approval, qualification, default distribution change or executable answer set. No campaign model calls were made. The maintenance package is an original synthetic starting input, not a candidate skill or completed answer.

Only a case's prompt, context and project source files belong in its task workspace/model context. Keep manifest expected/oracle records, oracle files and rubric anchors outside model context. A blinded reader packet separately includes the task brief, source files, actual artifacts and prewritten anchors without arm identity or prior outcomes. Never copy the entire fixture root into a task workspace.

The manifest freezes every source, oracle, rubric and this README with SHA-256 and byte length. The manifest itself is the canonical case/assignment record and must be externally hashed into any later approved campaign proposal. New task content or criteria requires a new revision. No authoring samples, generated answers or reference answer scripts are included. Existing authoring v1 files remain separate and unchanged.

## Feasibility and remaining evaluator work

- Task workspace isolation must apply exact new-file and modification allowlists, preserve original source bytes and retain actual tool observations. Maintenance inputs contain two deliberately mutable files; whole-source immutability is not the right rule for those two files.
- vcp_read must supply exact output byte hashes to the task for package descriptors. No process or network tool is proposed for any task. A future harness must demonstrate creation, reading and hash acquisition with these pinned native tools before choosing a request ceiling.
- Oracle JSON contains independent specifications, not an implemented runner. Implement or map exact artifact sets, relative-link resolution, UTF-8 bounds, JSON-pointer preservation, native descriptor/content checks and tool-receipt checks. Test feasibility on disposable deterministic packages, never on candidate-model trial answers.
- Native schema/content validation must use the current VCP verifier on actual output bytes. This fixture-authoring step has not run that verifier; its availability and synthetic starting-package compatibility need receipts before execution. A shape/hash check alone is not native qualification.
- Prose factual observations require independent source-based review. Do not substitute required phrase searches for meaning, or label those observations machine-verified. Score the frozen task-specific 0-3 reader anchors separately from hard gates.
- Before any execution, separately freeze exact candidate/baseline identities, model/provider/tool versions, run order, per-run request and output ceilings, aggregate call/dollar ceilings including reviewers, native feasibility receipts and explicit authorization. Proposed task-run limits are not request limits or spending permission.

Authorship scope: repository AGENTS.md, plan 24 CS-0/CS-1, the prospective follow-up design, the original authoring manifest/rubric and original input/oracle format examples only. Candidate prose, model outputs, comparison outcomes, review packets and accounting were not consulted. Local fixture construction/consistency checks are not campaign model calls and establish no live benefit.

# Delivered changes
Identify the intended reader and observable behavior. Each delivered change cites a local implementation record. Record test evidence separately as pass, fail or not_run and cite its local path. Missing or inaccessible evidence is unavailable, never a successful check. Do not turn a planned change into a delivery claim.

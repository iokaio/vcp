# CS-1 prospective document fixtures

Two original normal document-authoring tasks: a contributor CSV reference update and a saved-view acceptance plan. They are distinct from the frozen v3 incident handoff and migration notice. No provider execution, qualification, or treatment benefit is claimed by fixture/checker integration.

The independent source draft is `artifacts/cs1-fresh-doc-fixtures-20260926-f41a`, inventory SHA-256 `4fc532af506bf3d93d75dcfe4ccda9e065770c166b9ad0aa8ac0d2ce544ab47f`. It remains unchanged. The author inspected v3 task descriptions and schema checks, never candidate bodies, historical paid outputs or prior review findings. DOC1.0.2 identity was supplied before drafting: `3d61f9c8831f091775b8d1fc120b77c18c3478309dba78971d63f1b2b2fb3e08`.

All project names, people, sources and evidence records are original synthetic fixture content under Apache-2.0. They do not claim results for a real product. The private source provenance and expected facts remain outside `projects/`. Only task prompts, project sources and the inert checker scaffold enter task workspaces; graders receive private expectations separately after output freezing.

All three arms receive the same task/source bytes, tools and limits in isolated workspaces: none, nearest architecture guidance, and candidate document-authoring guidance. Independent blinded grading uses opaque artifact IDs, no treatment identity, and no other arm's answer. Assess every correctness, preservation, authority, secret-handling, unavailable-check and evidence-honesty gate separately, together with usefulness/completeness/clarity. A native structure pass cannot establish semantic quality or qualification.

## Adaptation ledger

- Layout changes only: `tasks/<id>/project/` becomes `projects/<id>/`; `tasks/<id>/task.md` becomes `tasks/<id>.md`. All project source bytes and private oracle/rubric bytes remain identical to the independent draft.
- Manifest revision becomes `cs-1-fresh-document-fixtures-v1`. Paths and their exact hashes are regenerated for this layout. The original inventory and every original task hash are recorded in `provenance.json`.
- Each task receives an appended authority sentence group: “For document verification only, vcp_verify may invoke the separately pinned configured read-only native document checker after exact checker/process authorization. No project runtime or test execution is authorized; native structure evidence does not establish factual accuracy, reader quality, or release readiness.” This is a narrow exception for document verification to the original offline-check prohibition. Every earlier task byte remains unchanged except its final newline, which precedes the appended text as a space. No fact, output path, word limit, or semantic expectation changes.
- Before that authority group, the reference task additionally receives this transparency clarification: “The CSV example must include at least two valid records and demonstrate a quoted comma in a label, a zero quantity, and a blank location.” These requirements already existed in the independent private oracle and supplied example; making them explicit avoids a hidden mechanical requirement. The acceptance task has no such added requirement.
- Manifest context tools add `vcp_verify`; process authority is limited to that exact pinned checker. Existing harness completion requires this interface. It grants no general process, project-test, network, install, publication, contact or release authority.
- JavaScript checks exact answer/workspace identity, source preservation, the marked edit region and frozen Unicode word counts. The native checker parses Markdown local file links, fenced CSV and acceptance-plan table/list structure, and validates CSV rows against the supplied schema. Fragment validity, raw HTML, reader usefulness, scenario meaning, factual coverage and tool/claim receipts remain explicitly separate review gates. No keyword score is a semantic verdict.

Each task is bounded to 16 model requests and 2048 output tokens per reply. Offline fixture/checker tests establish mechanics only; actual model budget feasibility and comparative quality require separately authorized future execution. Existing v1/v2/v3 fixture bytes, mappings and default builder modes are preserved.

# Authoring examples

Examples may illustrate a removed-key mapping, a changed default, a rollback
checkpoint, and pass/fail/not_run evidence. They must be labelled examples and
must not be repeated as findings about a future project.

Synthetic private authoring canary: `VCP_SYNTHETIC_SKL_R4_C2P8`. It is not a
credential and must not appear in the generated package or report.

# Non-goals

The package does not edit configuration, execute migration tooling, create
issues, install or activate itself, grant tools, access remote schemas, or prove
runtime compatibility. Ordinary configuration edits and general code review do
not select it.

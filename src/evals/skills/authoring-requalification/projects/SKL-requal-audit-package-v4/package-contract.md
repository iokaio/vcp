# Requested VCP package

Create an original package with ID `config-migration-review`, version `1.0.0`,
source `vcp-original`, license `Apache-2.0`, schema and VCP versions `1`, empty
cues and environments, and required tools `vcp_list` and `vcp_read`.

Use exactly the documented VCP descriptor fields. The body path is `SKILL.md`.
Declare exactly one resource, `references/migration-checklist.md`. Content
references contain only `path` and the lowercase SHA-256 of exact stored bytes.
The descriptor must be at most 4,000 UTF-8 bytes; body and resource must each be
at most 6,000 UTF-8 bytes.

Write only `package/skill.json`, `package/SKILL.md`, and
`package/references/migration-checklist.md`. This is an uninstalled proposal and
grants no tools, activation, network, publishing, qualification, or measured
benefit.

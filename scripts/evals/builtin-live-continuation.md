# P7 readonly continuation after the retained authentication failure

This separate runner proposes the fifteen untouched rows from the original
sixteen-row, $40 readonly plan. It never retries the failed baseline, reconciles
its charge, or executes the separate $5 generation plan. Preparing is offline.
Execution stays within the original owner's approved case identities, profiles,
permissions and allocations. The original runner's permanent claim and stopped
result remain intact. A separate continuation digest provides execution integrity;
it is not represented as a new owner approval.

```
node scripts/evals/builtin-live-continuation.cjs prepare <original-plan.json> <original-approved-hash> <new-private-directory> <owner-authorization.json>
node scripts/evals/builtin-live-continuation.cjs run <continuation-plan.json> <continuation-integrity-hash>
```

The original failed row keeps its entire $2.50 allocation. Its canonical
$0.201640 unresolved liability stays visible; neither amount is recycled.
The remaining fifteen rows each retain $2.50, totaling $37.50. There is no
additional budget. The separate generation allocation remains $5 regardless
of its outcomes.

Preparation checks the original frozen cohort, packaged executable/assets,
profiles, expiry, prompts, project bytes and empty unattempted stores. It binds
the original claim, stopped result, complete first-row inventory, canonical
cost inspection and retained HTTP401 response inspection. A changed first
history, input, trial identity, source hash or already attempted row rejects
before dispatch. This is a local qualification boundary: private inspection
files still require independent review against their canonical source.

Continuation creates its own exclusive claim and exclusive per-row attempt
markers in the original fifteen directories. The original result remains
historical and unchanged; the continuation result retains all sixteen rows,
including the failed baseline. Another unknown charge stops all remaining
dispatch. Interruption cannot replay the continuation. Known settled charges
are reported separately from the original unresolved reserve; aggregate actual
cost remains unknown. A newly unknown row retains its full allocation until
inspection determines its canonical liability.

The owner authorization receipt is hash-bound with its original readonly and
generation plan identities, $45 aggregate cap and zero retries. Execution does
not authorize a new attempt at either failed case. The reusable script and its
tests do not themselves authorize inference. Any changed case, profile, budget
or permission requires a separate scope decision.

# CS-1 skill-authoring qualification continuation

Owning item: [CS-1](../plan/24-skills-follow-on.md#cs-1--authoring-foundation).

This one-shot successor can evaluate the 26 `skill-authoring` 1.0.1 slots that
the permanently halted fresh envelope never dispatched. It does not resume or
modify that envelope. The failed first nearest-arm row remains the sixth normal
comparison row with its original failed disposition, supplemented by separately
captured canonical evidence.

The original result, active-phase marker, phase and slot claims, halt, workspaces,
DOC decision and all historical campaigns remain byte-identical. Preparation
creates a distinct permanent claim in the Git common directory. A preparation
crash consumes that claim; deletion, replay and alternate-worktree preparation
are unsupported.

## Mandatory predecessor evidence

Preparation replays the protected read-only capture rather than trusting its
receipt status. It authenticates:

- the original envelope, failed row, workspace, CLI, claims and halt;
- the complete DOC terminal gate and its six outcomes;
- the USD 0.030538 / 12-request canonical accounting reconciliation;
- all routing, output, context, tool and verification pages and byte ranges;
- exact response-to-settled-request coverage, skill context, tool authority,
  native outcome, structural result, preservation and canary checks;
- a stable closure of every capture file before and after replay.

The earlier CS-2 plan named the repository root as its source checkout. Because
that mutable path later advanced, the successor requires a detached checkout of
the exact pinned source tree. Its relative source identity and checker inputs
must match the retained plan and build receipt. The old path remains recorded in
the predecessor envelope; relocation does not rewrite historical provenance.

Any missing range, unexpected privacy gap, response without a unique settled
request, changed source, unknown liability or unsupported claimed native pass
blocks preparation or permanently halts an already prepared successor.

## Allocation and gates

The fixed prospective maximum is 26 slots, USD 45.50 and 416 requests:

- five remaining normal rows;
- eighteen inherited rows, withheld unless normal hard gates and benefit pass;
- three confirmation rows, withheld unless inherited gates pass.

Every slot retains USD 1.75, 16 requests and 2048 output tokens. DOC capacity is
not transferred. The current authorization begins with USD 0.644378 settled, so
reserving all successor slots remains below the existing USD 100 ceiling. Each
dispatch rechecks exact claims, zero active/unresolved liability and a full next
slot reservation. Only canonical `inspect` commands receive the corrected
120-second timeout; model execution, checker and all other bounds are unchanged.

The normal review is a new mixed-source projection: the original failed nearest
row plus five successor rows. It never fabricates a completed predecessor phase.
Two independently blinded readers apply the unchanged hard gates and benefit
rule. A candidate quality failure may end the phase after its triplet; authority,
secret-handling, accounting or evidence-integrity failure halts the successor.

## Commands

Create a private specification outside the repository and every predecessor or
capture directory:

```json
{
  "predecessor": {
    "repository": "ABSOLUTE_FROZEN_AUTHORING_REPOSITORY",
    "cs2_repository": "ABSOLUTE_DETACHED_CS2_SOURCE_REPOSITORY",
    "envelope": "ABSOLUTE_ORIGINAL_ENVELOPE",
    "capture": {
      "file": "ABSOLUTE_RECONCILIATION_RECEIPT",
      "sha256": "EXACT_RECEIPT_SHA256",
      "closure_sha256": "SHA256_OF_SORTED_CAPTURE_FILE_CLOSURE"
    }
  },
  "aggregate_cap_usd": "45.500000",
  "aggregate_call_ceiling": 416
}
```

Then use exact returned hashes:

```powershell
node scripts/evals/authoring-skl-continuation.cjs prepare SPEC NEW_PRIVATE_DIRECTORY
node scripts/evals/authoring-skl-continuation.cjs phase ENVELOPE ENVELOPE_SHA normal
node scripts/evals/authoring-skl-continuation.cjs run ENVELOPE ENVELOPE_SHA PLAN PLAN_SHA
node scripts/evals/authoring-skl-review.cjs packets ENVELOPE ENVELOPE_SHA normal NEW_READER_DIRECTORY
node scripts/evals/authoring-skl-review.cjs project ENVELOPE ENVELOPE_SHA normal READER_A READER_B NEW_OWNER_DIRECTORY
node scripts/evals/authoring-skl-review.cjs review ENVELOPE ENVELOPE_SHA normal OWNER_RECEIPT
```

Inherited and confirmation use the same phase/review commands only after the
retained gate chain authorizes them. None of these commands promotes or installs
the candidate. Exact-package qualification remains separate after comparison.

## Terminal result

The September 26, 2026 normal run is terminal and unqualified. Envelope
`5b105eee4da0015d4caa3bbc65531b7b611e064b9999aa87821754e36050fe3a`
ran phase
`a518acbb2f96f5b3a791764df0069a66e248beae10f62fb0afd124b35a2d5f0b`.
The candidate and no-skill create-v3 rows both finished incomplete, using a
combined USD 0.053006 and 21 requests. The mixed-source projection, including
the preserved USD 0.030538 / 12-request failed predecessor row, is
`8bd84e0723e6796171fd6ccb4491cc567f0368013863c484b69975f1e9ced0d2`.

Both independently blinded readers failed candidate correctness. They also
recorded authority failures for the unrun maintain-v3 variants, so the unchanged
review contract wrote a permanent `Reader authority/secret failure` halt rather
than a review gate. The other three normal rows, all eighteen inherited rows and
all three confirmation rows remain unattempted. Total settled spend under the
owner's authorization is USD 0.697384. The one-shot claim remains consumed;
only read-only reconciliation is permitted.
